- $id: config
  type: object
  properties:
    torrentingPort: { type: integer, format: ip-port }

    nginx:
      type: object
      properties:
        enabled: { type: boolean }
        serverNames:
          anyOf:
            - type: "null"
            - { type: string, format: nginx-server-name }
            - { type: array, items: { type: string, format: nginx-server-name }, minItems: 1, uniqueItems: true }

      additionalProperties: false
      required: [enabled, serverNames]

  additionalProperties: false
  required: [torrentingPort, nginx]
